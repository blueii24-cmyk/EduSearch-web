import React from 'react'

export default function FilterPanel({ filters, onChange, counts }) {
  const set = (key, value) => onChange({ ...filters, [key]: value })
  return <aside className="filter-panel"><div className="filter-title"><strong>Refine results</strong><button onClick={() => onChange({ type: 'All', distance: 'any', fresherFriendly: false, matchesSkills: false, eligible: false, jobType: 'all', collegeLevel: 'all' })}>Reset</button></div><label>Show me</label>{['All', 'College', 'Job', 'Internship'].map((type) => <button className={`filter-choice ${filters.type === type ? 'active' : ''}`} key={type} onClick={() => set('type', type)}>{type === 'All' ? 'All opportunities' : `${type}${type === 'College' ? 's' : type === 'Job' ? 's' : 's'}`}<span>{counts[type] || 0}</span></button>)}<label className="filter-label-spaced">Distance</label><div className="distance-row">{[['5', '5 km'], ['10', '10 km'], ['25', '25 km'], ['any', 'Any']].map(([value, label]) => <button className={filters.distance === value ? 'active' : ''} key={value} onClick={() => set('distance', value)}>{label}</button>)}</div><label className="filter-label-spaced">Only show</label><label className="check-filter"><input type="checkbox" checked={filters.fresherFriendly} onChange={(event) => set('fresherFriendly', event.target.checked)} /> Fresher friendly</label><label className="check-filter"><input type="checkbox" checked={filters.matchesSkills} onChange={(event) => set('matchesSkills', event.target.checked)} /> Matches my skills</label><label className="check-filter"><input type="checkbox" checked={filters.eligible} onChange={(event) => set('eligible', event.target.checked)} /> I’m eligible</label>{filters.type === 'Job' && <><label className="filter-label-spaced">Job type</label><div className="distance-row job-types">{['all', 'Full-time', 'Part-time', 'Remote'].map((type) => <button className={filters.jobType === type ? 'active' : ''} key={type} onClick={() => set('jobType', type)}>{type === 'all' ? 'Any' : type}</button>)}</div></>}{filters.type === 'College' && <><label className="filter-label-spaced">Course level</label><div className="distance-row"><button className={filters.collegeLevel === 'all' ? 'active' : ''} onClick={() => set('collegeLevel', 'all')}>Any</button><button className={filters.collegeLevel === 'Undergraduate' ? 'active' : ''} onClick={() => set('collegeLevel', 'Undergraduate')}>UG</button><button className={filters.collegeLevel === 'Postgraduate' ? 'active' : ''} onClick={() => set('collegeLevel', 'Postgraduate')}>PG</button></div></>}</aside>
}
